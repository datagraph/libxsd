Near-Term Tasks
===============

- xsd:date's value type must preserve the timezone, if one was given.

Nice to Have
============

- Remove the dependency on Boost.Regex once libstc++ supports std::regex.
